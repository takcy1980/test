{% extends "common/bootstrap.twig" %}

{% block title %}
    {{ labels.customer_login_page_title }}
{% endblock %}

{% block more_css %}
    <link href="/assets/common/css/default.css" rel="stylesheet"/>
    <link href="/assets/customers/css/login.css" rel="stylesheet">
{% endblock %}

{% block body %}
    <div class="container">
        {% if error is not empty %}
            <p class="error">{{ error }}</p>
        {% endif %}
        

            {% if error is not empty %}
                <p class="error">{{ error }}</p>
            {% endif %}
            <form class="form-signin" action="/app/customers/login" method="post">
                <h2 class="form-signin-heading">
                    {{ labels.customer_login_login_label }}:
                </h2>
                <label for="login" class="sr-only">
                    {{ labels.customer_login_loginname_label }}
                </label>
                <input type="text" id="login_name" name="login" class="form-control" placeholder="{{ labels.customer_login_loginname_label }}" required autofocus>
                <label for="password" class="sr-only">
                    {{ labels.customer_login_password_label }}
                </label>
                <input type="password" id="password" name="password" class="form-control" placeholder="{{ labels.customer_login_password_label }}" required>
                <button class="btn btn-lg btn-primary btn-block" type="submit" >
                    {{ labels.customer_login_login_button }}
                </button>

                <div style="border-top: 1px solid#888; padding-top:15px; font-size:85%"  >
                     {{labels.customer_login_no_account_label }}
                    <a href="/app/customers/login/new" >
                        {{labels.customer_login_signup_label }}
                    </a>
                </div>

            </form> 
      



    </div>

{% include "common/snippets/language_bar.twig" %}

{% endblock %}
